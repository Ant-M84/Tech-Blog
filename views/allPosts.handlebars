<div class="row">
    <div class="col">
        <h2>Greetings {{user.username}}!</h2>
    </div>
</div>
<br>

{{#if posts.length}}
<div class="card">
    {{#each posts as |post|}}
    {{> editItem post}}
    {{/each}}
</div>
{{/if}}